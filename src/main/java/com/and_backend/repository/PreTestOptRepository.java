package com.and_backend.repository;

import com.and_backend.preTest.PreTestOpt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PreTestOptRepository extends JpaRepository<PreTestOpt, Long> {
}
